import React from 'react';
import { View, Text, Image, TouchableOpacity } from 'react-native';

import { COLORS, FONTS, icons, SIZES, GOOGLE_API_KEY } from '../constants';

export const User = () => {
	return (
		<View>
			<Text style={{ ...FONTS.h1 }}>Usuario Screen</Text>
		</View>
	);
};
